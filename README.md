# example-eslint

eslint-plugin-vue

```sh
$ npm install --save-dev eslint-plugin-vue

$ cat .eslintrc
{
  "extends": ["eslint:recommended", "plugin:vue/recommended"],
  "plugins": ["vue"],
  "env": {
    "browser": true,
    "es6": true
  },
  "rules": {
  }
}
```
